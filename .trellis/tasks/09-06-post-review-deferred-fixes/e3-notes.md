# E3 排查记录（外部删除新建文件现象）

现象：会话期间新建的 3 个测试文件与 1 个临时钩子垫片被外部进程删除（后已重建并纳入提交）；
同日还观察到一次不属于当前会话的 `node --test` 进程（不同文件排序的重试）。

排查结论：
- 仓库内无任何删除 tests/ 的代码路径（grep 排查通过）。
- 挂起的 run-tests.cjs fast 进程（存活 7h46m，PID 53936）持有 dev.db 锁，是本机多个
  会话共用同一工作区 + 套件尾部挂起的组合结果；已清理。
- 最可能来源：多个 AI 会话并行操作同一仓库（存在会话重试/检查点回滚行为）或 IDE/网盘
  同步目录。建议：同一时间只保留一个活动会话；同步工具排除 .zcode/ 与 tests/。

另记：仓库中存在误提交的空文件 `server/p.$disconnect())`（65339262 引入），建议清理。
