# 站点登录门禁

本模块只证明“被允许进入当前部署实例”，不拥有用户表，也不隔离小说或设置。

- 凭证来自启动期环境变量，不进设置面板。
- `authMiddleware` 对 `/api` 全局生效；健康检查、登录状态和自动导演频道回调保持匿名。
- 桌面端 `AI_NOVEL_RUNTIME=desktop` 默认不要求门禁；Docker/production 非桌面默认要求，未配置账号密码时拒绝业务访问。
